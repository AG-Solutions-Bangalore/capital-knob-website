# Copy to `.env` and adjust per environment. Vite only exposes `VITE_*` vars.
VITE_API_BASE_URL=https://agsdemo.in/ckapi/public/api
# Optional auth header value sent as `Authorization` on every API request.
# VITE_SECRET_KEY=
